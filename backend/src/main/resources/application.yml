spring:

  datasource:
    url: jdbc:postgresql://database:5432/car_rental
    username: user
    password: user

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        jdbc:
          lob:
            non_contextual_creation: true

server:
  port: 8099